# Chrome 插件图标

由于 Git 不支持二进制文件，请自行准备以下尺寸的图标：

- `icon16.png` - 16x16 像素
- `icon48.png` - 48x48 像素  
- `icon128.png` - 128x128 像素

## 快速生成

可以使用在线工具或 AI 生成一个简单的机器人/放大镜图标。

## 临时方案

如果没有图标，Chrome 会显示默认拼图图标，插件功能不受影响。